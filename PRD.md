# Eko 产品需求文档

**产品名称**：Eko  
**版本**：v1.4  
**日期**：2026-04-24

---

## 1. 产品概述

### 1.1 背景与痛点

团队协作里，需求常常先出现在 IM 对话，但从“聊天”到“产出物”之间总有断层：

- 聊天内容需要手工整理
- 需要自己判断该输出文档、PPT 还是画板
- 资料分散在文档、Bitable 和群聊里，难以复用
- 产物没有回流到知识库
- 跨应用切换成本高
- 多人协作容易冲突，群聊信息会干扰创作

### 1.2 产品定义

Eko 是一款由 AI Agent 驱动的飞书原生办公工作台。它通过识别用户意图，结合三类飞书来源信息（飞书群聊上下文、飞书多维表格、RAG 知识库），自动决定是生成文档、PPT、画板，还是仅进行即时问答，并将结果通过飞书和网页工作台实时同步出去。

**唤醒方式**：在飞书群中 `@Eko + 一句指令`

### 1.3 核心设计哲学

- **意图驱动**：不要求用户先选格式，由 Agent 根据语义判断交付形态。
- **Agent 为主**：负责理解、检索、规划、执行。
- **GUI 为辅**：负责进度展示、人工微调和结果确认。
- **三源联动**：群聊、Bitable、RAG 三类信息可单独为空，但可组合增强结果。
- **闭环回流**：产物可再进入知识库和 Bitable 体系。
- **卡片驱动 + 独立工作台**：飞书卡片承接入口，网页工作台负责精修。

---

## 2. 核心功能模块

### 2.1 意图识别与分流

系统入口是飞书群 `@Eko` 或网页工作台输入。

Agent 会将输入分成四类：

| 分支 | 触发条件 | 动作 |
|------|----------|------|
| 闲聊 | 普通问答、事实确认、日常对话 | 直接回复文本 |
| 文档 | 方案、总结、说明、报告、纪要等 | 进入文档生成链路 |
| PPT | 汇报、展示、演示、路演等 | 进入 AI PPT 链路 |
| 画板 | 流程图、架构图、脑图、时序图等 | 进入画板链路 |

### 2.2 内容来源：三源联动

创作时可结合三类来源：

- **飞书群聊上下文**
  - 最近消息、讨论记录、当前任务语境
  - 可为空

- **飞书多维表格**
  - 项目排期、负责人、状态、结构化业务数据
  - 可为空

- **RAG 知识库**
  - 历史文档、模板、项目资料、经验沉淀
  - 可为空

### 2.3 卡片 + 工作台

- 飞书群触发后，系统返回交互卡片。
- 创建者可点击进入网页工作台继续编辑。
- 其他成员默认只读。
- AI 生成时，工作台会通过状态锁避免并发冲突。
- 结果可一键确认保存并归档。

### 2.4 产物生成

- **文档态**：输出结构化 Markdown / 文档内容。
- **PPT 态**：输出可编辑的 AI PPT 任务和页面结构。
- **画板态**：输出可视化布局和节点结构。
- **自动回流**：完成后进入知识库或归档流程。

### 2.5 实时同步

Redis Pub/Sub 负责广播：

- Agent 进度
- Session 状态
- 工具执行状态
- 飞书同步结果

---

## 3. 用户流程

1. 用户在飞书群中 `@Eko`
2. Eko 识别意图
3. 系统检索群聊、Bitable、知识库
4. Planner 生成步骤计划
5. 工具执行并实时广播状态
6. 用户在工作台微调结果
7. 确认保存后回流到飞书或 Bitable

---

## 4. 技术要求

### 4.1 技术栈

| 层级 | 技术选型 |
|------|---------|
| 前端 | Next.js, React, tldraw, Zustand |
| 后端 | FastAPI, SQLAlchemy |
| 检索 | Embedding, pgvector |
| 通信 | Redis Pub/Sub, WebSocket / SSE |
| 集成 | 飞书 OpenAPI, Bitable OpenAPI |

### 4.2 意图识别

- 使用 LLM 工具选择 + 本地规则兜底。
- 优先识别用户是否有明确动作词。
- 当前产物存在时，优先识别为编辑而不是新建。

### 4.3 三源内容获取

- 飞书群聊上下文：可为空
- Bitable：可为空
- RAG 知识库：可为空
- 检索结果会带来源信息，便于展示证据

### 4.4 实时同步

- 使用 Redis 作为实时总线
- 前端通过 WebSocket / SSE 接收状态
- 状态变化能在网页和飞书侧同时看到

---

## 5. 验收标准

- [ ] 能识别 `@Eko + 一句指令`
- [ ] 能区分闲聊、文档、PPT、画板意图
- [ ] 能从飞书群聊、Bitable、RAG 中取数
- [ ] 生成产物可回流到知识库或 Bitable
- [ ] 飞书卡片能打开独立工作台
- [ ] 创建者可编辑，其他成员只读
- [ ] 实时进度能同步到多端
- [ ] Redis 实时总线能广播状态

---

## 6. 相关文档

- [ARCHITECTURE.md](ARCHITECTURE.md)
- [API.md](API.md)
- [README.md](README.md)
