# Eko 产品需求文档

**产品名称**：Eko  
**版本**：v1.0  
**日期**：2026-04-24

---

## 1. 产品概述

### 1.1 背景与痛点

在团队协作中，需求往往始于 IM 对话。目前从"聊天"到"产出物"的过程存在断层，用户需要手动判断何时该写文档，何时该做演示，且跨应用切换繁琐。

### 1.2 产品定义

Eko 是一款基于 AI Agent 驱动的多端协同办公助手。它作为工作流的"主驾驶 (Pilot)"，通过识别用户意图，自动决定是生成 Word 文稿、PPT 画布，还是仅进行即时闲聊回复，实现三端实时同步与飞书生态闭环。

### 1.3 核心设计哲学

- **意图驱动 (Intent-Driven)**：用户无需指定格式，由 Agent 根据上下文判断最佳交付形态。
- **AI Agent 为主 (Pilot)**：负责理解意图、路由任务、执行创作。
- **GUI 为辅 (Co-pilot)**：负责进度反馈及多端结果呈现。

---

## 2. 核心功能模块

### 2.1 意图识别与分流中枢 (Scenario A/B)

这是系统的入口，Agent 会将 IM 输入归类为以下三种逻辑分支：

| 分支 | 触发条件 | 动作 |
|------|----------|------|
| **文稿创作 (Word 意图)** | 指令涉及方案梳理、长文总结、详细计划或逻辑论证 | 启动 Word 态生成逻辑，调用 RAG 检索背景资料 |
| **演示汇报 (PPT 意图)** | 指令涉及成果展示、产品介绍、会议汇报或视觉化诉求 | 启动画布态排版引擎，将核心逻辑视觉化 |
| **即时交互 (闲聊/QA 意图)** | 指令为简单询问、事实确认、日常闲聊或不含创作导向的对话 | Agent 直接在 IM 频道或 App 对话框回复文本，不触发文档生成或画布重绘，减少对协作界面的干扰 |

### 2.2 知识库与 RAG 引擎

- **后台管理**：支持 PDF/Docx 上传，后台自动分片与向量化。
- **事实增强**：在创作意图下，Agent 自动调取知识库；在闲聊意图下，Agent 优先基于历史上下文回复。

### 2.3 动态内容生成 (Scenario C/D)

- **Word 态 (线性逻辑)**：生成结构化 Markdown。
- **PPT/画布态 (视觉逻辑)**：Agent 驱动 Tldraw 自动布局。
- **生成决策逻辑**：Agent 可根据上下文同时触发 Word + PPT（综合汇报），或仅触发其中之一。

### 2.4 三端实时同步 (Scenario E)

**同步策略**：
- **创作分支**：全端广播，桌面端画布自动展开并开始"生长"。
- **闲聊分支**：仅在当前对话窗口显示，不打断桌面端的演示/创作状态。

### 2.5 飞书生态协同闭环 (Scenario G)

- **Bitable 同步**：仅针对"创作分支"产生的正式成果进行归档。

---

## 3. 用户流程 (User Flow)

```
1. 准备阶段
   └─ 用户在后台上传资料

2. 多态触发测试
   ├─ Case A (闲聊)
   │  └─ 用户问："咱们下午几点开会？"
   │  └─ Agent 识别为闲聊
   │  └─ 直接回复文本，画布不刷新
   │
   ├─ Case B (创作)
   │  └─ 用户说："帮我梳理下刚才讨论的运营方案。"
   │  └─ Agent 识别为文稿意图
   │  └─ 桌面端自动出现 Word 预览
   │
   └─ Case C (综合)
      └─ 用户说："把这个方案做成汇报材料。"
      └─ Agent 识别为演示意图
      └─ 桌面端切换至画布模式并自动绘图

3. Agent 创作过程
   ├─ 步骤 A：执行意图分类
   ├─ 步骤 B：若为创作，检索 RAG 知识库
   ├─ 步骤 C：后端生成对应 JSON 数据并写入数据库
   └─ 步骤 D：通过 Redis 广播状态，前端按分支渲染

4. 人工干预
   └─ 用户在手机端微调结果

5. 归档
   └─ 成果自动同步至飞书多维表格
```

---

## 4. 技术要求

### 4.1 技术栈规范

| 层级 | 技术选型 |
|------|----------|
| **前端** | Next.js, Tldraw SDK |
| **后端** | Python (FastAPI), SQLAlchemy |
| **数据/通讯** | PostgreSQL (pgvector), Redis (Pub/Sub) |

### 4.2 意图识别规格

- **分类模型**：使用 Function Calling 或结构化输出确保意图分类的稳定性。
- **决策权重**：优先继承最近 5 轮对话的上下文来判断当前是否进入"创作态"。

---

## 5. 分工与阶段里程碑

| 角色 | 职责 |
|------|------|
| **成员 A (前端)** | Dashboard、Tldraw 渲染、三端 UI |
| **成员 B (后端/AI)** | 意图分流逻辑、Agent 生成算法、飞书 API |
| **成员 C (架构/同步)** | Postgres/Redis 部署、同步逻辑、打包 |

---

## 6. 验收标准

- [ ] 能够准确区分"闲聊回复"与"文档/画布生成"。
- [ ] 演示从 IM 触发后，系统能根据语义自动选择产出 Word 还是 PPT。
- [ ] 闲聊模式下不应触发不必要的画布渲染动效。
- [ ] 任务状态及链接成功归档至飞书 Bitable。
