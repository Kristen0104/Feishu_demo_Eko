# Eko 产品需求文档

**产品名称**：Eko
**版本**：v1.4
**日期**：2026-04-24

---

## 1. 产品概述

### 1.1 背景与痛点

在团队协作中，需求往往始于 IM 对话，但从"聊天"到"产出物"的过程存在断层：
- 用户需要手动复制粘贴聊天记录来整理
- 需要自己决定是写文档还是做 PPT
- 过往的项目资料/模板分散在文档、Bitable 中，难以快速复用
- 生成的内容没有回流到知识库，下次还得重新来
- 跨应用切换繁琐
- 多人协作时容易冲突，群聊消息会干扰创作

### 1.2 产品定义

Eko 是一款基于 AI Agent 驱动的多端协同办公助手。它作为工作流的"主驾驶（Pilot）"，通过识别用户意图，结合三类飞书 API 获取的信息（飞书群聊上下文、飞书多维表格、RAG 知识库），自动决定是生成 Word 文稿、PPT 画布，还是仅进行即时闲聊回复，实现三端实时同步与飞书生态闭环。

**唤醒方式**：在飞书群中 @Eko + 一句指令

### 1.3 核心设计哲学

- **意图驱动（Intent-Driven）** - 用户无需指定格式，由 AI Agent 根据上下文判断最佳交付形态
- **AI Agent 为主（Pilot）** - 负责理解意图、获取三类信息（均来自飞书 API）、执行创作
- **GUI 为辅（Co-pilot）** - 负责进度反馈及多端结果呈现
- **三源联动创作** - 飞书实时聊天记录 + 飞书多维表格 + RAG 知识库有机结合（三类信息来源均可为空白，非强制）
- **闭环回流** - 生成的 Word/PPT 文档自动回到 RAG 知识库，不断积累
- **卡片驱动 + 独立工作台** - 飞书消息卡片触发，独立网页进行沉浸式编辑

---

## 2. 核心功能模块

### 2.1 意图识别与分流中枢（Scenario A/B）

这是系统的入口。**唤醒方式**：在飞书群中 @Eko + 一句指令

Agent 会将用户输入归类为以下三种逻辑分支：

| 分支 | 触发条件 | 动作 |
|------|---------|------|
| **文稿创作（Word 意图）** | 指令涉及方案梳理、长文总结、详细计划或逻辑论证 | 启动 Word 态生成逻辑，同时通过飞书 API 获取：群聊上下文 + Bitable 数据 + RAG 知识库（三类信息均可为空白） |
| **演示汇报（PPT 意图）** | 指令涉及成果展示、产品介绍、会议汇报或视觉化诉求 | 启动画布态排版引擎，同时通过飞书 API 获取：群聊上下文 + Bitable 数据 + RAG 知识库（三类信息均可为空白），将核心逻辑视觉化 |
| **即时交互（闲聊/QA 意图）** | 指令为简单问答、事实确认、日常闲聊或不含创作导向的对话 | Agent 直接在 IM 频道或 App 对话框回复文本，不触发文档生成或画布重绘，减少对协作界面的干扰 |

### 2.2 内容来源：三源联动 + 闭环回流（核心特色）

创作时的内容来源是**飞书群聊记录 + 飞书多维表格 + RAG 知识库**的有机结合（三类信息均可为空白，非强制），并且生成的内容会再回到知识库：

- **飞书群聊上下文**（飞书 API）：
  - 自动获取当前会话的最近聊天记录（默认最近 50 条，可配置）
  - 提取讨论中的关键信息、共识、待办事项
  - 总结实时对话内容，作为创作的基础素材
  - 可为空白

- **飞书多维表格**（飞书 API）：
  - 从绑定的 Bitable 中检索相关数据
  - 支持项目资料、活动记录、成员信息等表格数据的引用
  - 可将表格数据转化为图表/卡片融入 PPT 或文档
  - 可为空白

- **RAG 知识库**（飞书文档管理）：
  - 后台管理：支持 PDF/Docx/Markdown 上传，自动分片与向量化
  - 事实增强：基于用户查询，检索企业/项目已有的相关文档（存储在飞书文档）
  - 支持 API 调用：通过飞书 API 接入企业内部文档系统
  - **闭环回流**：生成的 Word/PPT 文档自动入库，作为后续创作的资料
  - 可为空白

- **三源结合创作**：
  - 飞书上下文提供"实时讨论内容"（刚才聊了什么）
  - Bitable 提供"结构化数据"（项目信息、数据记录）
  - RAG 知识库提供"过往经验/模板/资料"（以前怎么做的）
  - Agent 将三者有机融合，生成既有实时讨论、又有数据支撑、还有过往经验的内容
  - 三类信息来源均可为空白，不影响流程继续

### 2.3 卡片消息驱动 + 独立网页工作台（核心体验）

为了提供无缝的跨端体验并确保多用户协作的稳定性，Eko 采用**"卡片消息驱动 + 独立网页工作台"**的设计范式。

#### 2.3.1 消息卡片驱动的沉浸式工作流

- **无缝切入与交互** - 用户在飞书群首个下达 @Eko + 一句指令后，系统将立刻在群内回复一张结构化的"飞书交互卡片"。卡片内置动态进度和交互按钮（如"打开工作台"、"确认保存"等）
- **独立编辑环境** - 为避免群内其他聊天信息干扰 AI 的上下文，也防止核心产出物被群消息刷屏"冲掉"，所有的"精修编辑"功能均剥离出 IM 聊天框，统一收拢在卡片链接跳转的独立网页中。该网页内置专门的 AI 指令输入框
- **端侧差异化体验**：
  - **PC 端**：点击卡片后，支持以飞书侧边分屏或直接弹出独立窗口的形式，全屏展现 AI 的自动排版与创作过程
  - **移动端**：用户可根据当前场景，自主选择是否点击卡片链接进入工作台查看或操作

#### 2.3.2 极简权限隔离与并发控制

为规避多人实时编辑带来的服务器压力和逻辑冲突，系统采用单线程、强主导的权限管理机制：

- **创建者主导模式** - 在飞书群首个 @Eko 触发任务的用户自动成为该会话的"创建者"。只有创建者点开链接时，界面才会展示 AI 指令输入框并拥有全部修改权；其他群成员点开链接，默认进入"只读观摩"模式，只能查看实时进度
- **全局状态防冲突锁** - 系统配有"全局状态锁"机制。当 AI 正在接收指令思考或生成内容时，工作台的输入框及交互将被暂时锁定，确保整个流转过程单线程、无冲突，完美解决多人同时操作带来的系统并发压力

#### 2.3.3 多端自适应与毫秒级实时同步

- **底层状态同源** - 卡片链接本质是一个支持多端自适应的实时网页工作台，手机端和 PC 端底层共享同一个 WebSocket 状态通道
- **全场景无缝衔接** - 无论创建者是在通勤路上用手机打开链接输入指令微调，还是坐在工位上用电脑大屏直接操作修改与翻页，所有端的内容、元素状态、进度甚至光标位置，都会实现毫秒级的实时同步
- **一键闭环归档** - 在工作台中修改到满意后，创建者可直接点击卡片或工作台上的"确认保存"按钮，系统将自动把最终成果一键归档到飞书多维表格（Bitable），并更新群内卡片状态，完成整个业务流的闭环

### 2.4 动态内容生成（Scenario C/D）

- **Word 态（线性逻辑）** - 基于三类飞书信息（均可为空白），生成结构化 Markdown
- **PPT/画布态（视觉逻辑）** - 基于三类飞书信息（均可为空白），Agent 驱动 Tldraw 自动布局
- **生成决策逻辑** - Agent 可根据上下文同时触发 Word + PPT（综合汇报），或仅触发其中之一
- **自动回流** - 生成完成后，文档自动进入 RAG 知识库处理流程

### 2.5 三端实时同步（Scenario E）

**同步策略**：
- **创作分支** - 全端广播，桌面端画布自动展开并开始"生长"
- **闲聊分支** - 仅在当前对话窗口显示，不打断桌面端的演示/创作状态

### 2.6 飞书生态协同闭环（Scenario G）

- **Bitable 同步** - 仅针对"创作分支"产生的正式成果进行归档
- **Bitable 作为源** - 创作时可从 Bitable 读取数据作为素材

---

## 3. 用户流程（User Flow）

```
1. 准备阶段
   └─ 用户在后台上传知识库资料（项目文档、模板等）
   └─ 配置绑定飞书多维表格

2. 多态触发测试
   ├─ Case A（闲聊）
   │  └─ 用户在飞书群 @Eko："我们下午几点开会？"
   │  └─ Eko 识别为闲聊意图
   │  └─ 直接在群内回复文本，不弹出卡片
   │
   ├─ Case B（创作：Word）
   │  └─ 用户在飞书群 @Eko："帮我梳理一下刚才的讨论，结合上次的方案模板，写成文档。"
   │  └─ Eko 识别为 DOC 意图
   │  └─ 飞书群弹出交互卡片，显示"分析中"
   │  └─ Eko 同时通过飞书 API 获取三类信息：
   │     ├─ 群聊记录（可空白）
   │     ├─ Bitable 中的活动数据（可空白）
   │     └─ 知识库中的运营方案模板（可空白）
   │  └─ 创建者点击卡片，打开独立网页工作台
   │  └─ 桌面端自动展现 Word 预览，内容正在"生长"
   │  └─ 其他群成员点击卡片进入只读观摩模式
   │  └─ 创建者可在工作台输入新指令微调
   │  └─ AI 生成时，工作台自动上锁防止冲突
   │  └─ 完成后，文档自动回流到 RAG 知识库
   │
   └─ Case C（创作：PPT）
      └─ 用户继续在工作台或飞书群 @Eko："把这个方案做成汇报 PPT。"
      └─ Eko 识别为 PPT 意图
      └─ Eko 复用已有内容 + Bitable 数据 + 知识库资料（三类均可为空白）
      └─ 桌面端切换到画布模式并自动绘图
      └─ PPT 导出后也自动回流到知识库

3. Agent 创作过程
   ├─ 步骤 A：执行意图分类
   ├─ 步骤 B：通过飞书 API 获取群聊上下文
   ├─ 步骤 C：通过飞书 API 获取 Bitable 数据
   ├─ 步骤 D：通过飞书 API 检索知识库资料
   ├─ 步骤 E：后端生成对应 JSON 数据并写入数据库
   └─ 步骤 F：通过 Redis 广播状态，前端按分支渲染
   └─ 步骤 G：生成完成后，内容自动回流到 RAG 知识库

4. 人工干预
   └─ 创建者在手机端或 PC 端工作台微调结果
   └─ 其他群成员只读观摩

5. 归档
   └─ 创建者点击"确认保存"
   └─ 成果自动同步到飞书 Bitable
   └─ 群内卡片状态更新为"已完成"
```

---

## 4. 技术要求

### 4.1 技术栈规范

| 层级 | 技术选型 |
|------|---------|
| **前端** | Next.js, Tldraw SDK |
| **后端** | Python (FastAPI), SQLAlchemy |
| **数据/通讯** | PostgreSQL (pgvector), Redis (Pub/Sub) |

### 4.2 意图识别规格

- **分类模型** - 使用 Function Calling 或结构化输出确保意图分类的稳定性
- **决策权重** - 优先继承最近 5 轮对话上下文判断当前是否进入"创作态"

### 4.3 三源内容获取规格

- **飞书群聊上下文** - 通过飞书开放 API 获取会话历史，支持配置获取条数，可为空白
- **飞书多维表格** - 通过飞书开放 API 读取 Bitable 数据，支持配置表和视图，可为空白
- **RAG 知识库** - 通过飞书文档 API 管理，支持向量相似度检索，Top-K 可配置（默认 Top-5），可为空白
- **RAG 回流** - 生成的 Word/PPT 文档自动触发向量化入库流程

### 4.4 卡片与工作台技术规格

- **飞书交互卡片** - 使用飞书卡片消息 API，支持动态更新状态和按钮交互
- **独立网页工作台** - Next.js 单页应用，响应式设计支持多端自适应
- **WebSocket 实时同步** - Redis Pub/Sub 广播状态，多端共享同一状态通道
- **全局状态锁** - 后端维护状态锁，AI 生成期间防止多人并发操作
- **权限管理** - 区分"创建者"（可编辑）与"其他成员"（只读）

---

## 5. 验收标准

- [ ] 能够准确识别"@Eko + 一句指令"的唤醒方式
- [ ] 能够准确区分"闲聊回复"与"文档/画布生成"
- [ ] 演示从 IM 触发后，系统能根据语义自动选择产出 Word 还是 PPT
- [ ] **创作时能够通过飞书 API 获取三类信息：群聊记录、Bitable、RAG 知识库（三类信息均可为空白）**
- [ ] **生成的 Word/PPT 文档能够自动回流到 RAG 知识库，形成闭环**
- [ ] **@Eko 触发后能弹出飞书交互卡片，点击卡片能打开独立网页工作台**
- [ ] **创建者拥有编辑权限，其他群成员只读观摩**
- [ ] **AI 生成时工作台自动上锁，防止并发冲突**
- [ ] **手机端与 PC 端内容毫秒级实时同步**
- [ ] **点击"确认保存"后，成果自动归档到飞书 Bitable，卡片状态更新**
- [ ] 闲聊模式下不触发不必要的画布渲染动效
