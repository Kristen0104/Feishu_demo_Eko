# Eko 技术架构文档

**产品名称**：Eko  
**版本**：v1.0  
**日期**：2026-04-24

---

## 1. 总体架构图

项目采用 **"中心化 Agent + 实时总线 + 多端自适应 UI"** 的全栈架构。

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              多端展现层                                  │
├─────────────────────────────────────────────────────────────────────────┤
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐      │
│  │   桌面端 (Tauri) │  │  移动端 (Capacitor)│  │   网页端 (Web)   │      │
│  │  - Tldraw 画布   │  │  - 轻量预览       │  │  - Dashboard     │      │
│  │  - Word 预览     │  │  - 微调界面       │  │  - 全功能操作    │      │
│  └────────┬─────────┘  └────────┬─────────┘  └────────┬─────────┘      │
└───────────┼─────────────────────┼─────────────────────┼────────────────┘
            │                     │                     │
            └─────────────────────┼─────────────────────┘
                                  │
┌─────────────────────────────────┼────────────────────────────────────────┐
│                         实时总线层 (Redis)                              │
├─────────────────────────────────┼────────────────────────────────────────┤
│  ┌──────────────────┐         ┌──────────────────┐                      │
│  │   Pub/Sub 广播   │◄────────┤   Cache 缓存      │                      │
│  │  - 状态机变更    │         │  - 最近5轮对话    │                      │
│  │  - 画布更新      │         │  - Session 状态   │                      │
│  └──────────────────┘         └──────────────────┘                      │
└─────────────────────────────────┼────────────────────────────────────────┘
                                  │
┌─────────────────────────────────┼────────────────────────────────────────┐
│                         后端逻辑层 (FastAPI)                             │
├─────────────────────────────────┼────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                    Agent 引擎 (主驾驶)                             │  │
│  │  ┌────────────────────────────────────────────────────────────┐  │  │
│  │  │  状态机: IDLE → ANALYZING → RETRIEVING → GENERATING → COMPLETED │  │
│  │  └────────────────────────────────────────────────────────────┘  │  │
│  │  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐ │  │
│  │  │  意图分流模块    │  │  DocumentService │  │  CanvasService   │ │  │
│  │  │  - 决策树       │  │  - Word 生成      │  │  - Tldraw 布局   │ │  │
│  │  │  - Function Call│  │  - Markdown 输出  │  │  - 自动排版      │ │  │
│  │  └──────────────────┘  └──────────────────┘  └──────────────────┘ │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                          RAG 模块                                 │  │
│  │  - 文件提取: pypdf / docx2txt                                    │  │
│  │  - 向量化: Embedding API                                         │  │
│  │  - 检索: pgvector Cosine Similarity                              │  │
│  └──────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────┼────────────────────────────────────────┘
                                  │
┌─────────────────────────────────┼────────────────────────────────────────┐
│                         数据存储层 (PostgreSQL)                          │
├─────────────────────────────────┼────────────────────────────────────────┤
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐      │
│  │   主业务表       │  │   pgvector 向量  │  │   JSONB 画布快照  │      │
│  │  - 用户表        │  │  - 文档分片向量  │  │  - Tldraw 状态    │      │
│  │  - Session 表    │  │  - 索引          │  │  - 版本时间戳     │      │
│  │  - 成果归档表    │  └──────────────────┘  └──────────────────┘      │
│  └──────────────────┘                                                    │
└─────────────────────────────────────────────────────────────────────────┘
                                  │
┌─────────────────────────────────┼────────────────────────────────────────┐
│                         飞书生态集成                                    │
├─────────────────────────────────┼────────────────────────────────────────┤
│  ┌──────────────────┐  ┌──────────────────┐                            │
│  │  飞书 IM Bot     │  │  飞书 Bitable    │                            │
│  │  - 消息接收      │  │  - 成果归档      │                            │
│  │  - 回复发送      │  │  - 状态同步      │                            │
│  └──────────────────┘  └──────────────────┘                            │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 技术栈详细说明

### 2.1 前端展现层 (Next.js)

| 组件 | 技术选型 | 说明 |
|------|----------|------|
| **UI 框架** | Next.js 15 (App Router) | 利用 Server Components 优化首屏 |
| **状态管理** | Zustand | 用于处理跨端的轻量级状态同步 |
| **画布引擎** | Tldraw SDK | 负责 PPT 态的渲染、自动布局计算及导出 |
| **动画** | Framer Motion | 实现 Agent 思考过程中的组件流式载入和生长动效 |

### 2.2 后端逻辑层 (FastAPI)

| 组件 | 技术选型 | 说明 |
|------|----------|------|
| **核心框架** | Python FastAPI | 异步非阻塞架构 |
| **Agent 引擎** | 自研 Agent 类 | 基于 DeepSeek 模型执行任务拆解与 Function Calling |
| **RAG 模块 - 提取** | pypdf / docx2txt | 文档内容解析 |
| **RAG 模块 - 向量化** | Embedding API | 文本向量化 |
| **RAG 模块 - 检索** | pgvector | 基于 Cosine Similarity 搜索 |

### 2.3 存储与通讯层

| 组件 | 技术选型 | 说明 |
|------|----------|------|
| **主数据库** | PostgreSQL 14+ | 关系型数据库 |
| **向量存储** | pgvector | 存储 RAG 向量 |
| **画布快照** | JSONB | 存储 Tldraw 画布的动态快照 |
| **实时总线** | Redis | 消息与缓存 |
| **Pub/Sub** | Redis | 处理多端状态变更的瞬时广播 |
| **Cache** | Redis | 存储最近 5 轮对话上下文，加速 Agent 推理 |

### 2.4 跨端打包方案

| 端 | 技术选型 | 说明 |
|----|----------|------|
| **桌面端** | Tauri | 将 Web 内容编译为本地桌面应用，利用 Rust 层处理高性能文件操作 |
| **移动端** | Capacitor | 将 Web 内容封装为 App，通过原生插件调用手机存储和震动反馈 |

---

## 3. 关键架构设计

### 3.1 "主驾驶"状态机

Agent 在后台维护一个状态机，每一步动作都会通过 Redis 广播：

```
IDLE (空闲)
  │
  ▼
ANALYZING (意图分析中)
  │
  ▼
RETRIEVING (知识库检索中)
  │
  ▼
GENERATING (内容生成中)
  │
  ▼
SYNCING (多端同步中)
  │
  ▼
COMPLETED (已完成)
```

**状态广播机制**：
- 每次状态变更触发 Redis Pub/Sub 消息
- 消息包含: `session_id`, `state`, `progress`, `payload`
- 前端订阅对应 Session 频道，实时更新 UI

### 3.2 离线一致性策略

| 场景 | 处理逻辑 |
|------|----------|
| **WebSocket 断开时** | 前端将更新记录在本地状态 (Zustand + IndexedDB) |
| **重连后** | 前端对比 Postgres 的版本时间戳，执行"最后写入者胜"的合并逻辑 |
| **冲突解决** | 以 `updated_at` 字段为准，较新的修改覆盖较旧的 |

### 3.3 意图分流决策树

```
1. 输入特征提取
   ├─ 关键词匹配
   └─ 历史 Session 标签继承

2. LLM 判别
   └─ 通过提示词约束，输出结构化的意图 ID
      ├─ ID 1: 闲聊/即时交互
      ├─ ID 2: 文稿创作 (Word)
      └─ ID 3: 演示汇报 (PPT/Canvas)

3. 路由执行
   ├─ ID 1 → 调用 ChatResponse (直接返回文本)
   ├─ ID 2 → 调用 DocumentService (生成 Markdown)
   └─ ID 3 → 调用 CanvasService (生成 Tldraw JSON)
```

**决策权重**：优先继承最近 5 轮对话的上下文来判断当前是否进入"创作态"。

---

## 4. 部署与伸缩

### 4.1 开发环境

使用 Docker Compose 一键启动：

```yaml
services:
  fastapi:
    build: ./backend
    ports:
      - "8000:8000"
  postgres:
    image: pgvector/pgvector:pg14
    environment:
      POSTGRES_DB: eko
  redis:
    image: redis:7-alpine
```

### 4.2 生产环境

- **云平台**：阿里云 / AWS
- **API 层**：水平伸缩，多实例负载均衡
- **数据库**：PostgreSQL 主从复制
- **Redis**：哨兵模式高可用

---

## 相关文档

- [产品需求文档 (PRD.md)](PRD.md)
- [README.md](README.md)
